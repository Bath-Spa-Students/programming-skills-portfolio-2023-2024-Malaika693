def make_shirt(size, message):
    print (f"The size of the shirt is {size} and the message prinyted on it")

# Call the function once using positional arguments
make_shirt("small", "I love myself!")

# Call the function a second time using keyword arguments
make_shirt(size="medium", message="Keep calm and code on!")

